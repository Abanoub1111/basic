[pytest]
testpaths = tests
pythonpath = src .
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -ra --strict-markers
markers =
    database: needs --run-database and TEST_DATABASE_URL (migrated PostgreSQL)
    ai: needs --run-ai and GROQ_API_KEY (paid network calls)
