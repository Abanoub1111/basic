HTTP/LangGraph       Waiters accepting requests
Application          Manager coordinating the work
Domain               Rules and vocabulary of the restaurant
Infrastructure       Kitchen equipment and external suppliers
Bootstrap            Person who opens the restaurant and connects everything



| File | Responsibility | Existing code that will move there |
|---|---|---|
| `domain/models.py` | Core email concepts | Email and classification definitions |
| `application/ports.py` | Contracts for things the application needs | “I need something capable of classifying emails” |
| `application/services.py` | Coordinates the use case | Classify, then respond/notify/ignore |
| `infrastructure/groq_classifier.py` | Implements classification using Groq | `init_chat_model()` and `with_structured_output()` |
| `infrastructure/langchain_response_agent.py` | Implements responses and tool calls | `bind_tools()`, `llm_call()`, tool execution |
| `interfaces/langgraph/state.py` | LangGraph-specific state | `State` and `StateInput` |
| `interfaces/langgraph/nodes.py` | Functions LangGraph invokes | `triage_router()`, `tool_node()`, `should_continue()` |
| `interfaces/langgraph/graph.py` | Connects LangGraph nodes | `StateGraph`, edges, and compilation |
| `interfaces/http/schemas.py` | Validates HTTP JSON | FastAPI/Pydantic request and response models |
| `interfaces/http/routes.py` | Defines URLs | `POST /emails/process` |
| `interfaces/http/app.py` | Creates the FastAPI application | `app = FastAPI()` |
| `bootstrap.py` | Constructs and connects real objects | Connect Groq implementations to application services |
| `__init__.py` | Marks a directory as a Python package | Usually stays empty |




domain me3rf4 7aga 3n application
application me3rf4 7aga 3n infrastructure
